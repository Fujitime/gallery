<a href="{{ url()->previous() !== url()->current() ? url()->previous() : '/' }}" class="hidden lg:block dark:text-white dark:bg-gray-800">
    <div class="w-10 h-10 dark:text-white dark:bg-gray-800 flex items-center justify-center">
        <span class="text-7xl font-extrabold">←</span>
    </div>
</a>
